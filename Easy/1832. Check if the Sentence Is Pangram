class Solution(object):
    def checkIfPangram(self, sentence):
        """
        :type sentence: str
        :rtype: bool
        """
        letters = set()
        
        for letter in sentence:
            letters.add(letter)
            
            if len(letters) == 26:
                return True
            
        return False
